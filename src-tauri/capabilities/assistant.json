{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "assistant",
  "description": "Capability for the assistant window",
  "windows": ["assistant"],
  "permissions": ["core:default", "dialog:default"]
}
